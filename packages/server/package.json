{
  "name": "@nyaya-mitra/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "API surface for Nyaya Mitra; holds the Gemini key so the client never sees it",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "start": "node --env-file-if-exists=.env dist/index.js",
    "dev": "tsc -b && node --env-file-if-exists=.env dist/index.js"
  },
  "dependencies": {
    "@google/genai": "^2.22.0",
    "@nyaya-mitra/core": "0.1.0",
    "express": "^5.2.1",
    "zod": "^4.6.5"
  }
}
